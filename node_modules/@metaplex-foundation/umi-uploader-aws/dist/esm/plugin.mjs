import { createAwsUploader } from './createAwsUploader.mjs';

const awsUploader = (client, bucketName) => ({
  install(umi) {
    umi.uploader = createAwsUploader(client, bucketName);
  }
});

export { awsUploader };
//# sourceMappingURL=plugin.mjs.map
