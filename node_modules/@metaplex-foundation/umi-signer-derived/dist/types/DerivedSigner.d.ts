import { Context, KeypairSigner, Signer } from '@metaplex-foundation/umi';
export type DerivedSigner = KeypairSigner & {
    readonly originalSigner: Signer;
};
export declare const createDerivedSigner: (context: Pick<Context, 'eddsa'>, originalSigner: Signer, message: string | Uint8Array) => Promise<DerivedSigner>;
export declare const isDerivedKeypair: (signer: Signer & {
    secretKey?: Uint8Array;
    originalSigner?: Signer;
}) => signer is DerivedSigner;
